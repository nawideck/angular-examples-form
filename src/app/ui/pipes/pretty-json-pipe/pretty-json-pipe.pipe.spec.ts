import { PrettyJsonPipePipe } from './pretty-json-pipe.pipe';

describe('PrettyJsonPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyJsonPipePipe();
    expect(pipe).toBeTruthy();
  });
});
