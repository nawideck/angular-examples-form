import { FieldErrorInfo } from './field-error-info.model';

describe('FieldErrorInfo', () => {
  it('should create an instance', () => {
    expect(new FieldErrorInfo('test', 'test')).toBeTruthy();
  });
});
